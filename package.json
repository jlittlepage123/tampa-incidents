{
  "name": "tampa-incidents",
  "version": "1.0.0",
  "description": "Tampa Police incidents map with historical tracking",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "fetch": "node -e \"require('./src/fetcher').fetchAndStore()\""
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "express": "^4.21.0",
    "node-cron": "^3.0.3",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
